{% from '_base.j2' import identity_block, concept_block, creative_direction_block, business_block, insights_block, structure_block, conversion_block, tech_block with context %}
Crea una web fresca, editorial y comercial para "{{ business.name }}", una tienda
de ropa en {{ business.address }}. Debe parecer una experiencia de marca, no un
catalogo basico.

{{ identity_block() }}

{{ concept_block() }}

{{ creative_direction_block() }}

{{ business_block() }}

{{ insights_block() }}

{{ structure_block() }}

DETALLES:
  - Imagenes grandes, ritmo editorial y mucho cuidado en espaciado.
  - Animaciones suaves: hover en prendas, transiciones de lookbook y reveal al scroll.
  - No mostrar importes si no se proporcionan; usar "Consultar disponibilidad".

{{ conversion_block() }}

{{ tech_block() }}
