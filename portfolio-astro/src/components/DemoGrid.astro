---
import { demos } from "../data/demos";
const base = import.meta.env.BASE_URL;
---
<section class="section" id="trabajos">
  <div class="section-head split reveal">
    <div>
      <p class="label">Trabajos</p>
      <h2>Trabajos de referencia, no plantillas.</h2>
    </div>
    <p>Estos ejemplos enseñan distintas formas de resolver una web: más elegante, más directa, más visual o más funcional. La tuya puede ir en otra dirección.</p>
  </div>
  <div class="demo-grid">
    {demos.map((demo) => (
      <article class="demo-card reveal">
        <a class="demo-cover" href={`${base}${demo.href}`} aria-label={`Ver ejemplo de ${demo.title}`}>
          <img src={`${base}${demo.image}`} alt="" loading="lazy" decoding="async" />
          <span>{demo.visual}</span>
        </a>
        <div>
          <p class="eyebrow">{demo.sector}</p>
          <h3>{demo.title}</h3>
          <p>{demo.summary}</p>
          <div class="tags">{demo.tags.map((tag) => <span>{tag}</span>)}</div>
          <a class="text-link" href={demo.href}>Ver ejemplo</a>
        </div>
      </article>
    ))}
  </div>
</section>
